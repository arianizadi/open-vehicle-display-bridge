# Tesla Adapters

Tesla-specific decoders and display/touch configuration are selected only by an
exact authenticated manifest. Unknown arbitration IDs, OTA changes, counters,
and conflicting signals fail closed.

Open DBCs are candidate evidence, not automatic support.
