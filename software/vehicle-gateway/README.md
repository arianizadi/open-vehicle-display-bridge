# Vehicle Gateway

Transforms pinned, provenance-rated read-only frames into normalized,
timestamped, quality-tagged signals. It never exposes a raw CAN transmit handle
to UI, Android applications, plugins, or projection software.
