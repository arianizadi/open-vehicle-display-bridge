# UI Shell

The initial UI is intentionally distinct from Tesla's native safety interface,
Park-only, local-first, and explicit about signal quality/age. It will not mimic
gear, warning, telltale, speed, or reversing-camera authority.
