# Hardware in the Loop

HIL fixtures are generated from the same signed manifest pinned by firmware.
They verify connector continuity, source switching, route feedback, timing,
power, touch, CAN receive behavior, and the de-energized native path.
