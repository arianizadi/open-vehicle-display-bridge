# Bench Automation

Future tools will control current-limited supplies and test-pattern sources,
capture measured state, and produce signed reports. Commands default to outputs
off and require an explicit manifest hash before energizing hardware.
