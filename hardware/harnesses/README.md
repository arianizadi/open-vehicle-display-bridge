# Harnesses

Harnesses are exact-revision adapters generated from reviewed manifests. Each
design records connector source, keying, orientation, terminals, pin map,
shielding/impedance, length, conductor rating, labels, and fixture tests.

No universal Tesla harness is planned. A physical identity should be readable
by the supervisor, and a direct factory coupler remains the rollback path.
