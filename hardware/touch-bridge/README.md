# Touch Bridge Hardware

Planned hardware preserves the native Highland sideband path while native video
is selected and exposes verified local touch as USB HID only in local mode.
Routing is exclusive and break-before-make, under the independent supervisor.

The differential-I2C/touch protocol and electrical implementation remain bench
unknowns. No synthetic touch is injected into Tesla UI.
