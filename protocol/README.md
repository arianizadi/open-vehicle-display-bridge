# Protocol

This path contains revision manifests, normalized signal definitions, small
privacy-scrubbed test vectors, and independently documented observations.

Raw CAN/video/touch captures remain outside Git. No active CAN command set is in
scope for the initial project. DBC content must include provenance and cannot be
assumed stable across an over-the-air update.
