# Test Vectors

Only minimal synthetic or privacy-scrubbed vectors belong here. Each vector
records target manifest, acquisition method, expected result, negative/control
case, original artifact SHA-256, and redaction review.
