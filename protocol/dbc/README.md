# DBC Policy

No DBC is trusted wholesale. Candidate definitions are pinned to an immutable
upstream commit, licensed/attributed, then validated signal by signal at the
chosen connector with target profile and Tesla software version.

Only curated receive signals enter a release adapter.
