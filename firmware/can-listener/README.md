# CAN Listener Firmware

Discovery firmware filters and timestamps received frames but has no transmit
API. A minimal independent decoder can provide Park/reverse state to the
supervisor only after per-profile validation, counters, freshness, and conflict
handling are proven.
