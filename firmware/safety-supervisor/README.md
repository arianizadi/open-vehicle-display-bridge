# Safety Supervisor Firmware

The independent supervisor owns video/touch selection, hardware identity,
watchdog, Park-only gates, fault latching, and native fallback. It accepts only
narrow authenticated/versioned requests from untrusted compute.

Reset, bootloader, update, stale inputs, unknown configuration, and any fault
select native by hardware truth table as well as firmware policy.
